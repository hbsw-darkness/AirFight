using UnityEngine;
using System.Collections;

public class PointZone : MonoBehaviour {
    //중복 리스폰 방지를 위한 체크 메소드
    public bool pointZoneCheck = false;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
